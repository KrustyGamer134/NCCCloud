[pytest]
# asyncio_mode = auto makes every async test and async fixture work without
# requiring an explicit @pytest.mark.asyncio decorator.
asyncio_mode = auto

# Only look for tests inside the tests/ directory.
testpaths = tests

# Short tracebacks keep output readable; add -v on the CLI when you need full details.
addopts = --tb=short

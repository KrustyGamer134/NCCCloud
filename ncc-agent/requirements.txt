# Remote agent dependencies
websockets==13.1
python-dotenv==1.0.1
bcrypt==4.2.0
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
packaging>=24.0

# The NCC core (core/ and plugins/) must be installed as a local path dependency.
# Run the following from the repo root before starting the agent:
#
#   pip install -e /path/to/NewControlCenter
#
# This makes `core` and `plugins` importable from the NCC workspace.
